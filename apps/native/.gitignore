# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `expo prebuild` (CNG) — not committed.
# NOTE: prebuild regenerates ios/; re-apply signing (DEVELOPMENT_TEAM + CODE_SIGN_STYLE)
# per NATIVE_PLAN.md, or set the team in Xcode. ios/ here is ~2.3GB (Pods).
ios/
android/

# Metro
.metro-health-check*

# Debug / logs
*.log
npm-debug.*

# macOS
.DS_Store
*.pem

# local env
.env*.local

# Supabase config (EXPO_PUBLIC_* — anon key)
.env
